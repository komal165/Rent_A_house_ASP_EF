@page
@model Rent_A_House.Pages.HouseOwners.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>House Owners</h2>

<p>
    <a class="btn btn-success" asp-page="Create">Add New </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HouseOwner[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HouseOwner[0].ContactNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.HouseOwner) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactNumber)
            </td>
            <td>
                <a class="btn btn-success" asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a class="btn btn-success" asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a class="btn btn-success" asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
