@page
@model Rent_A_House.Pages.Houses.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>House</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="House.Id" />
            <div class="form-group">
                <label asp-for="House.HouseOwnerId" class="control-label"></label>
                <select asp-for="House.HouseOwnerId" class="form-control" asp-items="ViewBag.HouseOwnerId"></select>
                <span asp-validation-for="House.HouseOwnerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="House.HouseAddress" class="control-label"></label>
                <input asp-for="House.HouseAddress" class="form-control" />
                <span asp-validation-for="House.HouseAddress" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-success" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
